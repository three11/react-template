/**
 * External dependencies
 */
import React from 'react';

/**
 * Render the component
 */
const Shell = ({ children }) => {
	return <div className="o-shell">{children}</div>;
};

export default Shell;
